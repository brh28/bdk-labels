# TODO

## CLI Commands

- [x] `import` — Import BIP329 JSONL from stdin or a file
- [x] `add` — Create a new annotation
- [x] `describe` — Set the description (and optionally origin) on an annotation
- [x] `list` — Query and list stored annotations
- [x] `tag` — Add tags to an annotation
- [ ] `rm` — Remove tags from an annotation, or delete it entirely with `--force`
- [ ] `export` — Export annotations as BIP329 JSONL

## Version Control

- [ ] View change history
- [ ] Rollback changes

## Features 

- [ ] Add an --override to import, or else only add new annotations 
